{
  "name": "pa-fhir-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "POC: Medicare LCD coverage-policy PDF -> Neo4j graph -> Temporal human-gated review -> Da Vinci CRD/DTR FHIR artifacts",
  "license": "MIT",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 'src/**/*.test.ts' 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "neo4j-driver": "^5.28.3",
    "unpdf": "^1.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.2"
  }
}
