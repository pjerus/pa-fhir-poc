# Checkpoint — 2026-08-21

## Completed this session

### M6 + polish (overnight, commits `e486c3e..9f907b3`)
- **M6 closed, evidence-first**: `cli.ts validate <lcdId>` runs pinned
  validator_cli.jar 6.10.2 in a Dockerized Temurin 21 JRE (no host Java;
  `tools/fetch-validator.sh` once, IG cache in gitignored `.fhir/`). The
  validator surfaced and we fixed two generic gaps:
  `Questionnaire.subjectType ["Patient"]` (1..1 in `dtr-base-questionnaire`)
  and dom-6 narratives (`src/fhir/narrative.ts`). Flags `-tx n/a` and
  `-allow-example-urls` are deliberate and documented — do not "fix".
- **Polish**: committed `docs/examples/`, mermaid README rewrite
  (cold-reader order), LICENSE (MIT), `docs/backlog.md` with the
  fhir-zod-gen seam.

### Publication (no repo commits; GitHub state)
- Repo public at **github.com/pjerus/pa-fhir-poc** (MIT detected, topics set,
  homepage → pjerus.github.io). LICENSE attributed to Pat Jerus (`8ee3261`).
- Personal site **pjerus.github.io** built and live (separate repo
  `pjerus/pjerus.github.io`): project entries for pa-fhir-poc (with
  CMS-0057-F link + Jan 1 2027 deadline, inline SVG flow diagram),
  fhir-zod-gen (repo flipped public, linked), Pantastic (unlinked, early).
  Search Console verified, sitemap submitted, indexing requested.

### Second LCD (commits `eda252f..7bc696a`) — generality proven
- **L33718 (PAP/OSA) + A52467** through the full chain, fixtures-only.
  Both LCDs: DTR Questionnaire PASS 0/0 against `dtr-std-questionnaire`;
  PlanDefinition PASS 0 errors base R4; CRD skip by design.
- The second document exposed three generic defects, fixed with tests:
  1. `sections.ts` — hard-wrapped PDF fragments matched heading heuristics
     (dropped L33718's adherence requirement). Headings now start with a
     capital and name their section within the first 3 words.
  2. `structure.ts` — categories were undefined in the prompt (~90%
     labeled "limitation"); one-line definitions added.
  3. Covered codes were prose-harvested (both articles' code sections are
     `N/A`); now explicit code tables only, LCD table ∪ article table.
     Extraction snapshots gained `hcpcsCodes`.
- **Both ground truths re-pinned** after line-by-line review against the
  source PDFs: L33822 = 67 requirements (34/28/5), 31 codes; L33718 = 105
  (49/51/5), 22 codes. The old L33822 counts (37/…, 20 codes) depended on
  the false-heading bug and prose harvest.
- Suite **132/132** incl. both live acceptance gates; typecheck clean;
  conformance evidence: `docs/conformance/L33718.md` (+ L33822 re-verified).

## Open PRs / issues
- None. No branches, no PRs — all work fast-forwarded on `main` and pushed
  (`origin/main` = `7bc696a`).

## In-flight branches
- None; tree clean.

## Workers/processes to restart on resume
- None stale. Review worker stopped after the approvals. `pa-fhir-poc-neo4j`
  container healthy, leave up — holds BOTH approved subgraphs (L33822,
  L33718). Ollama: system service. Shared Temporal :7233 ns `pa-fhir-poc`.

## Findings parked for a decision
- **DTR questionnaire sourcing** (`docs/backlog.md`): both MACs'
  DOCUMENTATION REQUIREMENTS sections are shared boilerplate, so both
  questionnaires have 5 near-identical items; the clinical attestations are
  indication-category and only reach the CRD card. Enriching the
  questionnaire from indications is a deliberate M4 design change — decide
  before doing.

## Next steps
1. Optional: **health-plan PA document experiment** (commercial payer
   policy through the pipeline) — feasibility discussed: needs a second
   heading profile in `sections.ts`, no article pairing (already optional),
   copyright care (don't commit the PDF; minimal quotes in expected.json;
   CPT is AMA-licensed).
2. Optional: extraction quality metrics (precision/recall vs expected.json)
   for the README.
3. Uptake: share to chat.fhir.org Da Vinci stream / LinkedIn post; watch
   Search Console Performance.
