# Checkpoint — 2026-08-20

## Completed this session
- Repo bootstrapped (git init, TS/ESM scaffold, Node ≥22.18 type stripping), M1→M3 built and merged to `main`.
- M1: extract pipeline (`pdf-text` → `sections` → LLM `structure` → snapshot) + hardening for real MCD layout (layered-text collapse, recurring-label rejection, revision-history cutoff). Acceptance gate green vs live qwen3.8:27b, twice (37 reqs, 19/14/4, 5 source-derived key phrases).
- M2: Neo4j layer (schema/write/validate/read, `cli.ts load`) + article extractor (`extract-article`: deterministic ICD-10/HCPCS parsing, LLM denial reasons). Real A52464: 461 ICD-10, 20 HCPCS (Coding Guidelines fallback — article's CPT/HCPCS section is "N/A"), 16 denial reasons.
- M3: Temporal review workflow (propose→validate→signal-gated commit|compensate), worker, client, `review-start`/`review-signal` verbs. Verified on shared Temporal :7233 ns `pa-fhir-poc`.
- Full chain ran on real docs: `load L33822 A52464` → clean validation → review approved by "pat". Graph now holds approved L33822: 37 REQUIRES, 20 COVERS, 461 LISTS, 16 DEFINES + provenance props.
- `src/` scrubbed of all document-specific codes (fabricated stand-ins in tests; `Z79.4` kept deliberately as the not-in-article exclusion case).

## Open PRs / issues
- None (no remote configured; local-only repo).

## In-flight branches
- None — everything merged; working tree clean at `14b1233` on `main`.

## Workers/processes to restart on resume
- None stale. Temporal worker (`node src/workflow/worker.ts`) is NOT running — start it only when driving a review. `pa-fhir-poc-neo4j` container: healthy, leave up (holds the approved L33822 subgraph). Ollama: system service.

## Next steps
1. **M4 — FHIR projection**: `cli.ts project <lcdId>` via `readApprovedSubgraph` → `out/<id>.{crd,dtr,plandefinition}.json`; `src/fhir/profiles.ts` holds all Da Vinci canonical URLs; **emit CodeableConcept with one-element coding arrays, never bare Coding** (translation bookmark, CLAUDE.md Open questions); verify HCPCS canonical system URI against spec, don't recall it.
2. M5 — `cli.ts run` glue + README (incl. "add a second LCD" section).
3. Session convention if continuing agentically: Sonnet implementers, Fable reviews diffs directly, SDD ledger under `.superpowers/sdd/`.

## 23:35 update — M4 + M5 complete; done bar met

### Completed this session
- **M4** (commits `3789f90..e7f29a9`): `src/fhir/` (profiles/crd/dtr/plandefinition/project) + `cli.ts project`. Canonicals spec-verified (THO v7.3.0, DTR IG v2.2.0): HCPCS `http://www.cms.gov/Medicare/Coding/HCPCSReleaseCodeSets`, ICD-10-CM `http://hl7.org/fhir/sid/icd-10-cm`; only the DTR Questionnaire carries `meta.profile` (`dtr-std-questionnaire`) — CRD card and PlanDefinition carry none, verified absences. Acceptance on real graph: 14 DTR items / 20 PD actions.
- **M5** (commits `51be356..5c59055`): `cli.ts run` (extract → review workflow → blocking human gate → projection; `awaitReview` in client.ts) + `README.md` (fresh-clone walkthrough, second-LCD section). Verified live end-to-end: run → approve signal → exit 0, `out/L33822.*` regenerated.
- Suite 112/112 incl. ~40s live-LLM gate (re-validated the fresh acceptance-run snapshots). Fix of note: test-fixture code VALUES must be `TEST-P-`-prefixed so shared-graph cleanup catches Code nodes (leak found live, cleaned, `f644614`).
- CLAUDE.md updated per milestone; `Code.system` open question resolved (short names in graph, URIs at projection).

### Open PRs / issues
- None (no remote; local-only repo).

### In-flight branches
- None — all merged ff to `main` @ `5c59055`, tree clean, milestone branches deleted.

### Workers/processes to restart on resume
- None. Review worker was started for the M5 acceptance run and stopped after; nothing stale. `pa-fhir-poc-neo4j` container healthy, leave up (holds approved L33822). Ollama: system service.
- Session env note: the harness Agent tool was blocked all session (iTerm2 `it2` check); `it2` is now installed (pipx + user site-packages), so a fresh session should dispatch normally. Workaround used: headless `claude -p --model sonnet` background jobs.

### Next steps
1. POC done bar is met (M1–M5). Remaining work is optional: **M6** (stretch, labeled) — validate emitted resources against real Da Vinci CRD/DTR StructureDefinitions.
2. Optional polish: second-LCD demonstration run to exercise README's generic claim with a real second policy.
3. If publishing: repo is README-ready; add a remote + push.
